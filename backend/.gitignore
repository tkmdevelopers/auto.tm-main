# ----------------------------------------------------------------------
# BACKEND (NESTJS) IGNORES
# ----------------------------------------------------------------------

# Dependencies
node_modules/

# Build Output
dist/
build/
coverage/
*.tsbuildinfo

# Application Data
uploads/
*.seed

# Environment
.env
.env.test
.env.production
!.env.example

# Docker / Deployment
docker-compose.override.yml
.docker/
*.tar
images_bundle.tar
postgres_16.tar

# Lockfiles (Currently ignored based on previous config - Recommended to un-ignore for consistency)
package-lock.json
yarn.lock
pnpm-lock.yaml
